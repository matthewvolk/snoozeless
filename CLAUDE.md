## Package Manager

- Use `pnpm` for all package management commands

## Check Work

- Use `pnpm fix && pnpm typecheck && pnpm test` to check your work after making changes

## Testing

- Unit tests: Colocate as `*.spec.ts` files next to implementation files in `src/`
- API tests: Place in `test/` folder as `*.spec.ts` files
